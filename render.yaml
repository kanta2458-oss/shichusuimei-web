services:
  - type: static
    name: shichusuimei-web
    buildCommand: npm install --no-audit --no-fund && npm run build:web
    staticPublishPath: dist-web
    envVars:
      - key: VITE_GOOGLE_MAPS_API_KEY
        sync: false
